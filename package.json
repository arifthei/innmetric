{
  "name": "innmetric-site",
  "private": true,
  "description": "innmetric.com static site",
  "type": "module",
  "scripts": {
    "lint:copy": "node scripts/lint-copy.mjs",
    "deploy": "npm run lint:copy && npx --yes netlify-cli deploy --prod --dir ."
  }
}
