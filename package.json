{
  "name": "innmetric-site",
  "private": true,
  "description": "innmetric.com static site",
  "type": "module",
  "scripts": {
    "lint:copy": "node scripts/lint-copy.mjs",
    "deploy": "npm run lint:copy && netlify deploy --prod --dir ."
  },
  "devDependencies": {
    "netlify-cli": "^23.11.1"
  }
}
